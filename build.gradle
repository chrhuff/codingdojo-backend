apply plugin: 'war'
apply plugin: 'java'

configurations {

}

repositories {
    flatDir { dirs "lib" }
    mavenCentral()
}

dependencies {

    compile group: 'org.slf4j', name: 'slf4j-api', version: '1.7.12'

    //common dependencies
    //common "org.slf4j:slf4j-api:1.7.12"
    //common "org.slf4j:slf4j-log4j12:1.7.12"
    //common 'log4j:log4j:1.2.14'

}

war {
    from 'src/main/webapp' // adds a file-set to the root of the archive
}
